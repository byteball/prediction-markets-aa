{
	messages: [
		{
			app: 'payment',
			payload: {
				asset: 'base',
				outputs: [
					{ address: "{ trigger.data.to ? trigger.data.to : trigger.address }" }
				]
			}
		},
		{
			app: 'data',
			payload: {
				define: 1,
				factory: `{trigger.data.factory}`,
			}
		},
	]
}
